# oxfmt 默认读取 .gitignore 与 .prettierignore（见 oxfmt --help 的 --ignore-path 说明）。
# apps/zcode-cli 自带 oxfmt（更高版本）与 format/format:check 脚本；根仓的 oxfmt
# 不应跨目录改写其文件，否则两边格式化器会互相打架。
apps/zcode-cli/

# 这两个 fixture 是 dynamic-workflow 子包的图测试输入，刻意使用 for await 的非法 TS 语法，
# oxfmt 解析会报错；只排除这两个文件，其余 graphs fixture 仍参与格式检查。
apps/zcode-cli/packages/dynamic-workflow/tests/graphs/control-flow-for-await.ts
apps/zcode-cli/packages/dynamic-workflow/tests/graphs/async-promise-for-await-of.ts

# server/：从 Reactor-Desktop 搬来的独立 pnpm workspace（服务端）。它不是本仓的 workspace
# 成员，代码按「零改动搬迁」保留原仓格式，不参与根仓 oxfmt —— 否则一次全仓格式化会让服务端
# 与新仓的 diff 无法对照，也让旧仓的验证结论失效。
server/
